const ERROR = {
    NOT_GIT_REPOSITORY: 'fatal: not a git repository (or any of the parent directories): .git'
}

const CONSOLE_PAINT = {
    // yellow
    INFO: '\x1B[33m%s\x1b[0m:'
};

exports = {
    ERROR,
    CONSOLE_PAINT
};
